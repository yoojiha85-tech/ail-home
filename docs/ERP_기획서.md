# 허니문 여행사 ERP 기획서
### 상담기록 → 견적서 → 예약 → 계약서 → 예약기록 → 예약의뢰서 → 정산 통합 시스템

작성일: 2026-09-19 · 대상: (주)아일항공여행사 허니문 사업부

---

## 1. 목표

현재 상담-견적-예약-계약-정산이 **엑셀·수기·메신저로 분산**되어 있어 다음 문제가 발생합니다.

- 상담 이력이 담당자 개인 자료에만 남아 인수인계·재상담 시 정보 유실
- 견적서 버전 관리가 안 되어 "고객에게 어떤 금액을 보냈는지" 혼선
- 예약 확정 후 계약서·예약의뢰서(항공/리조트 발주)가 별도 문서로 관리되어 누락 위험
- 정산 시 원가·판매가·수수료를 재계산해야 해서 시간 소모 크고 오류 발생

**ERP의 핵심 목표**는 고객 1건의 문의가 들어온 순간부터 정산 완료까지 **하나의 데이터(예약 건)로 끝까지 연결**되어, 상담원이 반복 입력 없이 다음 단계로 넘어갈 수 있게 하는 것입니다.

---

## 2. 전체 업무 흐름 (End-to-End)

```
① 상담기록 → ② 견적서 → ③ 예약(가예약) → ④ 계약서 → ⑤ 예약기록(확정) → ⑥ 예약의뢰서(발주) → ⑦ 정산
```

| 단계 | 이 단계에서 하는 일 | 담당 | 상태값(예시) |
|---|---|---|---|
| ① 상담기록 | 고객 문의 접수, 희망 목적지/예산/일정/인원 기록 | 상담원 | 신규 → 상담중 → 견적대기 |
| ② 견적서 | 상담 내용 기반 항공+리조트+객실+식사플랜 견적 산출, 고객 발송 | 상담원 | 작성중 → 발송완료 → 수정요청 |
| ③ 예약(가예약) | 고객이 견적 수락 → 항공/리조트 좌석·객실 임시 확보(Hold) | 상담원 | 가예약 → 확정대기 |
| ④ 계약서 | 여행계약서(약관, 취소규정, 결제조건) 발행 및 서명 | 상담원/고객 | 발송 → 서명완료 |
| ⑤ 예약기록 | 계약 완료 건을 정식 예약으로 전환, 예약번호 부여 | 시스템 자동 | 확정 |
| ⑥ 예약의뢰서 | 항공사/리조트/랜드사에 발권·바우처 요청 문서 발송 | 예약팀 | 의뢰발송 → 컨펌수신 |
| ⑦ 정산 | 원가/판매가/수수료 계산, 입금·출금 매칭, 마진 확정 | 경리/관리자 | 정산대기 → 정산완료 |

핵심은 **③~⑦이 모두 "하나의 예약 건(Booking ID)"에 연결**된다는 점입니다. 즉 상담기록에서 시작된 데이터가 견적서 번호, 예약번호, 계약서번호, 정산번호로 옆으로 계속 이어집니다(엑셀로 비유하면 "고객관리" 시트의 한 행이 "견적", "예약", "계약", "정산" 시트로 고유번호를 통해 서로 연결되는 구조입니다).

---

## 3. ERP 모듈 구성 (7개 모듈 + 2개 마스터 모듈)

### 업무 모듈
1. **CRM/상담관리** – 고객 정보, 상담 이력, 문의 채널(전화/카톡/홈페이지)
2. **견적관리** – 견적서 작성, 버전 관리, 고객 발송 이력
3. **예약관리** – 가예약/확정예약, 좌석·객실 Hold 상태 추적
4. **계약관리** – 전자계약서, 약관·취소규정, 서명 상태
5. **예약의뢰(발주)관리** – 공급사(항공사/리조트/랜드사)별 발주서, 컨펌 회신 관리
6. **정산관리** – 원가/판매가/마진, 입금·출금, 미수금·미지급금
7. **리포트/대시보드** – 상담원별 실적, 목적지별 판매량, 월별 매출·마진

### 마스터 데이터 모듈 (다른 모듈이 참조하는 기준 정보)
8. **상품 마스터** – 목적지, 리조트, 객실타입, 시즌, 요금표, 취소규정
9. **공급사/파트너 마스터** – 항공사, 랜드사, 리조트 담당자·연락처·정산조건

---

## 4. 데이터베이스 설계 (핵심 테이블)

> 참고: Table=엑셀 시트, Row=한 줄의 데이터, Column=항목, PK=고유번호, FK=다른 표와 연결하는 연결고리 라고 생각하시면 됩니다.

### 4-1. 핵심 테이블 관계도

```
customers(고객)
   └─< consultations(상담기록)
           └─< quotes(견적서) ─< quote_items(견적상세: 항공/리조트/객실/식사)
                   └─< bookings(예약)
                           └─1 contracts(계약서)
                           └─1 booking_confirmations(예약기록/확정)
                           └─< supplier_requests(예약의뢰서) ─> suppliers(공급사)
                           └─1 settlements(정산) ─< payments(입출금내역)

products_resorts / room_types / seasons / rate_plans   ← 견적/예약이 참조하는 마스터 데이터
```

### 4-2. 테이블별 주요 항목

| 테이블 | 주요 컬럼(항목) | 설명 |
|---|---|---|
| `customers` | 고객ID(PK), 이름, 연락처, 예식일, 유입경로 | 최초 문의 고객 |
| `consultations` | 상담ID(PK), 고객ID(FK), 상담일시, 희망목적지, 예산, 인원, 상담메모, 담당자 | 상담 이력 누적(여러 건) |
| `quotes` | 견적ID(PK), 상담ID(FK), 버전번호, 총금액, 유효기한, 상태 | 같은 상담에서 여러 버전 견적 가능 |
| `quote_items` | 항목ID(PK), 견적ID(FK), 구분(항공/리조트/객실/식사/옵션), 상품ID(FK), 단가, 수량, 원가, 판매가 | 견적의 상세 라인 |
| `bookings` | 예약ID(PK), 견적ID(FK), 예약번호, 상태(가예약/확정/취소), 출발일, 인원 | 견적이 확정되면 생성 |
| `contracts` | 계약ID(PK), 예약ID(FK), 계약서번호, 약관버전, 서명일, 서명상태, 결제조건 | 전자서명 연동 |
| `booking_confirmations` | 확정ID(PK), 예약ID(FK), 항공PNR, 리조트컨펌번호, 확정일 | "예약기록"의 실체 |
| `supplier_requests` | 의뢰ID(PK), 예약ID(FK), 공급사ID(FK), 의뢰유형(발권/바우처), 의뢰일, 회신상태, 회신번호 | 공급사별로 여러 건 가능 |
| `suppliers` | 공급사ID(PK), 구분(항공사/리조트/랜드사), 담당자, 연락처, 정산조건 | 마스터 |
| `settlements` | 정산ID(PK), 예약ID(FK), 총매출, 총원가, 마진, 정산상태, 정산확정일 | 예약 1건당 1개 |
| `payments` | 결제ID(PK), 정산ID(FK) 또는 예약ID(FK), 구분(입금/출금), 금액, 일자, 방법 | 분할결제/환불 다건 |
| `products_resorts` / `room_types` / `seasons` / `rate_plans` | 목적지, 리조트명, 객실타입, 시즌구간, 시즌별 요금 | 견적 산출의 기준 데이터 |

### 4-3. 설계 시 유의점 (검수 관점)

- **중복 최소화**: 리조트/객실/요금은 마스터 테이블에서 한 번만 관리하고, 견적/예약은 그 값을 참조(FK)만 하도록 설계 → 시즌 요금이 바뀌어도 마스터 한 곳만 수정
- **이력 보존**: `quotes`는 UPDATE가 아니라 버전을 늘려 INSERT (고객에게 "몇 번째 견적을 보냈는지" 추적 가능해야 함)
- **상태값 일원화**: 상담/견적/예약/정산 상태를 각 테이블에 문자열로 흩어놓지 말고, 공통 상태 코드 테이블로 관리 → 리포트 집계 오류 방지
- **안전한 삭제**: 예약이 하나라도 연결된 고객/상품/공급사는 물리삭제(DELETE) 금지, 비활성화(휴면처리) 컬럼으로 대체
- **권한 분리(RLS)**: 상담원은 본인 담당 고객만 조회, 관리자는 전체 조회, 정산 원가/마진 컬럼은 경리·대표만 조회 가능하도록 권한 분리 (고객용 화면에는 원가·마진 절대 노출 금지)

---

## 5. 화면(UI) 흐름 – 상담원 기준

상담속도를 높이는 것이 최우선이므로, **한 화면에서 다음 화면으로 "이어쓰기"** 가 되도록 설계합니다.

1. **고객 상세 화면** 진입 → 상단에 고객정보, 하단 탭에 [상담이력 | 견적 | 예약 | 계약 | 정산] 순서로 배치
2. 상담 작성 후 **"견적서 만들기" 버튼** 클릭 → 상담 내용(목적지/인원/예산)이 자동으로 견적 화면에 채워짐 (재입력 없음)
3. 견적 확정 후 **"예약 전환" 버튼** → 예약 건 자동 생성, 예약번호 자동 채번
4. 예약 확정 후 **"계약서 발행" / "예약의뢰서 발송" 버튼**이 같은 화면에 나란히 노출 → 클릭 한 번으로 문서 생성
5. 계약+공급사 컨펌이 모두 완료되면 **"정산 대상"으로 자동 이동** (상담원이 별도로 옮길 필요 없음)

> 클릭 수 최소화 기준: 상담기록→견적서 1클릭, 견적→예약 1클릭, 예약→계약서/의뢰서 각 1클릭. 즉 문의 접수부터 정산 대상 등록까지 상담원이 반복 입력하는 항목이 없어야 합니다.

---

## 6. 개발 로드맵 (MVP → 2단계 → 장기 확장)

### MVP (1~2개월, 핵심만)
- 상담기록 + 견적서 (버전관리) + 예약(가예약/확정) 3단계만 우선 구축
- 계약서/예약의뢰서는 **PDF 양식 자동생성**(전자서명 없이 다운로드/카톡전송) 수준으로 시작
- 정산은 예약 건별 원가·판매가 입력 후 마진 자동계산 정도만 (엑셀 내보내기 지원)
- 상담원 3~5명 규모, Supabase 기반 웹 어드민으로 구축

### 2단계 (3~6개월)
- 계약서 **전자서명 연동** (예: 카카오 전자서명, 모두싸인 API)
- 예약의뢰서 **공급사 자동 발송**(이메일/API) + 회신 상태 추적
- 정산 자동화: 입금/출금 매칭, 미수금 알림, 월마감 리포트
- 상담원별/목적지별 실적 대시보드

### 장기 확장 (6개월~)
- 다른 여행사에도 판매 가능한 **SaaS형 ERP**로 확장 (테넌트 분리, 여행사별 상품/요율 커스터마이징)
- 리조트·랜드사와의 **실시간 재고·요금 연동(API)** → 견적 자동 산출
- 데이터 누적 기반 **가격 최적화/프로모션 추천** (인기 시즌·리조트 통계 활용)
- 고객용 모바일 웹으로 견적 확인, 계약서 서명, 결제까지 셀프서비스 확장

---

## 7. 예상 리스크 및 확인 필요 사항

- **환율/유류할증료 변동**: 견적 유효기한이 지나면 재계산이 필요 → 견적서에 유효기한·환율기준일 명시 필수
- **시즌 경계일 처리**: 성수기/비수기 경계에 걸친 일정은 요금이 이원화될 수 있음 → 마스터 시즌 테이블 설계 시 날짜 겹침 검증 로직 필요 (확인 필요)
- **공급사 정산 주기 상이**: 항공사(선결제) vs 랜드사(후정산) 등 조건이 달라 정산 모듈에서 공급사별 정산조건 컬럼 필수
- **전자계약서 법적 효력**: 사용할 전자서명 서비스가 전자문서법상 효력을 인정받는지 확인 필요 (확인 필요)

---

## 8. 실제 사용 서식 분석 및 필드 매핑

기존에 실제 사용 중인 엑셀 서식(1개 파일, 6개 시트: `허니문 계약서` · `예약및정산` · `예약의뢰서` · `예약취소의뢰서` · `호텔예약의뢰서` · `보험가입`)을 검토했습니다. 아래는 시트별 실제 항목을 위 DB 설계(4장)에 매핑한 결과이며, 향후 화면 설계·테이블 컬럼 확정의 기준 자료로 사용합니다.

### 8-1. 시트 ↔ ERP 모듈 ↔ DB 테이블 매핑

| 엑셀 시트 | ERP 모듈(3장) | 대응 테이블 | 비고 |
|---|---|---|---|
| 허니문 계약서 | 계약관리 | `contracts` (+ `bookings`, `customers`) | 여행조건·특별약관(취소규정)·환율조항까지 한 장에 포함된 실사용 계약서 |
| 예약및정산 | 예약관리 + 정산관리 | `bookings`, `booking_confirmations`, `supplier_requests`(AGT), `settlements`, `payments` | 예약·정산이 한 시트에 같이 있어 현재는 두 업무가 분리되지 않은 상태 |
| 예약의뢰서 | 예약의뢰(발주)관리 | `supplier_requests` | 랜드사/호텔에 보내는 신규 예약 요청 |
| 예약취소의뢰서 | 예약의뢰(발주)관리 | `supplier_requests` (의뢰유형=취소) | 취소 요청 전용 양식, 신규 의뢰서와 컬럼 구조 거의 동일 |
| 호텔예약의뢰서 | 예약의뢰(발주)관리 | `supplier_requests` (공급사구분=호텔) | 체크인/체크아웃만 별도로 특화된 축약형 |
| 보험가입 | 예약의뢰(발주)관리 | `supplier_requests` (공급사구분=보험사) | 여행자보험 가입 의뢰 전용 |

### 8-2. 시트별 실제 필드 → DB 컬럼

**허니문 계약서 → `contracts` / `bookings` / `customers`**

| 엑셀 항목 | 제안 컬럼 |
|---|---|
| 여행상품, 여행기간(출발~도착), 예식일 | `bookings.product_name`, `departure_date`, `return_date`, `customers.wedding_date` |
| 1인요금, 총액, 신청금, 잔액, 최종결제일 | `contracts.unit_price`, `total_price`, `deposit_amount`, `balance_amount`, `final_payment_due` (총액·잔액은 저장값이 아니라 **계산식**으로 관리 → 4-3 원칙과 동일) |
| 입금계좌(은행/계좌번호/예금주) | `contracts.payment_account` (또는 회사 마스터 공통값 참조) |
| 신랑/신부 성명(한글/영문), 전화번호, 이메일, 주민번호 | `customers` 1건당 2명(신랑/신부) 구조 필요 → `booking_travelers`(예약ID+역할[신랑/신부/보호자 등]+개인정보) 테이블 별도 분리 권장 |
| 호텔, 식사(조식/중식/석식 횟수), 쇼핑유무, 여행보험 가입내용 | `quote_items` (구분=호텔/식사/보험) |
| 특별약관(취소료 규정 전문), 환율 조항, 기타(사은품·할인·고정환율 안내) | `contracts.terms_text`, `contracts.exchange_rate_clause`, `contracts.gift_promo_text` — **약관은 손질 없이 그대로 보여줘야 하므로 마스터 약관 템플릿에서 스냅샷으로 복사 저장** (약관이 나중에 바뀌어도 이미 발행된 계약서 문구는 안 바뀌어야 함) |
| 담당자(서명), 여행자 서명란 | `contracts.staff_signer`, `contracts.customer_signature_status` |

**예약및정산 → `bookings` / `booking_confirmations` / `supplier_requests` / `settlements` / `payments`**

| 엑셀 항목 | 제안 컬럼 |
|---|---|
| 계약일, 예약담당, 설명회일, 현지취소마감 | `bookings.contract_date`, `staff_id`, `briefing_date`, `local_cancel_deadline` |
| 신랑/신부 이름·영문명·주민번호·전화번호·여권수령일 | `booking_travelers` (여권수령일 `passport_receive_date` 포함) |
| AGT(랜드사)·담당·전화·수배일·항공취소마감·현지취소마감·취소여부 | `supplier_requests` (공급사구분=랜드사) |
| 호텔명 × 3, 박수 | `quote_items` (구분=호텔, 다건 가능하도록 반복 구조로 이미 설계됨) |
| 항공예약(예약일/PNR/CRS/발권티엘/발권일), 항공 스케줄 원문, 별도요금(유류할증료) | `booking_confirmations.pnr`, `crs_office`, `ticketing_office`, `ticketing_date`, `flight_schedule_raw`(원문 텍스트 그대로 저장), `quote_items`(구분=유류할증료) |
| 인천호텔(호텔명/객실타입/투숙일) — 출국 전날 숙박 | `quote_items` (구분=인천숙박, 출발지 숙박도 상품 구성요소로 관리) |
| 판매가/할인/계약가/별도요금/총요금/합계 | `settlements.sale_amount`, `discount_amount`, `contract_amount`, `extra_amount`, `total_amount` — **전부 수식으로 자동계산**(현재 엑셀도 수식으로 되어 있음, 8-3 참고) |
| 입금 내역(계약금/중도금/사은품대체 등, 날짜, 금액, 결제수단) vs 지출 내역 | `payments` (구분=입금/출금) |
| 사은품(가방/앨범 등), 추천인, 포함사항/현지포함/특이사항 | `bookings.gift_items`, `referral_source`, `notes` |
| 정산(항공/AGT/호텔/가방/앨범/보험/기타 원가 + 수익) | `settlements.cost_air`, `cost_land`, `cost_hotel`, `cost_gift`, `cost_album`, `cost_insurance`, `cost_etc`, `profit_amount`(=총요금-원가합계, 자동계산) |

**예약의뢰서 / 예약취소의뢰서 / 호텔예약의뢰서 / 보험가입 → `supplier_requests`**

| 엑셀 항목 | 제안 컬럼 |
|---|---|
| 발신(자사)/수신(공급사)/담당자, 예약일 | `supplier_requests.sender`, `supplier_id`(FK), `sender_staff`, `receiver_staff`, `request_date` |
| 여행자 성별/한글명/영문명/타이틀(MR·MS)/연락처 | `booking_travelers`를 그대로 참조 (재입력 없이 예약 건에서 자동 인용) |
| 상품명, 출발일, 도착일, 항공정보, 호텔정보(복수) | `bookings` / `quote_items`를 참조 (재입력 없음) |
| 체크인/체크아웃 (호텔예약의뢰서 전용) | `supplier_requests.check_in`, `check_out` |
| 취소 대상 리조트/풀빌라명 + 박수 (취소의뢰서 전용) | `supplier_requests.cancel_target_item`, `cancel_nights` |
| 여행기간·국가 (보험가입 전용), 보험플랜(예: "2억원 플랜, 휴대품손해 50만원") | `supplier_requests.insurance_plan_text` |
| 기타 요청사항 (자유서식 텍스트) | `supplier_requests.remarks` |
| "확약 팩스 또는 메일 부탁드립니다" 안내문 | 공급사별 고정 문구는 발송 템플릿에 자동 삽입 (반복 타이핑 불필요) |

### 8-3. 기존 서식에서 그대로 가져올 좋은 설계 (유지 권장)

- **수식 기반 자동계산**: 현재 엑셀도 총액=1인요금×2, 잔액=총액-신청금, 최종결제일=출발일-45일, 수익=총요금-원가합계 등을 전부 수식으로 관리하고 있습니다. ERP에서도 이 계산은 **DB 트리거/뷰 또는 화면 계산 로직으로 그대로 자동화**하면 됩니다(상담원이 손계산할 필요 없음).
- **문서 간 연동(참조) 구조**: `예약및정산` 시트가 `허니문 계약서` 시트 값을 셀 참조(`='허니문 계약서'!G1` 등)로 그대로 끌어오고 있습니다 — 이는 ERP가 지향하는 "한 번 입력, 계속 재사용" 구조와 정확히 같은 설계 의도입니다. ERP에서는 이걸 FK 참조로 구현하면 됩니다.
- **여러 건 반복 가능한 구조가 필요한 항목**: 호텔(최대 3개+인천호텔), 사은품, 입금/지출 내역은 이미 여러 줄로 반복 입력되고 있어 → DB에서도 1:N 테이블(`quote_items`, `payments`)로 설계한 것이 맞습니다.

### 8-4. 검수 중 발견된 오류·불일치 (확인 필요)

첨부해 주신 파일은 실제 고객 정보가 담긴 완료 건이라 데이터 오류 몇 가지가 발견되어, 서식을 그대로 디지털화하기 전에 짚어드립니다.

| 항목 | 발견 내용 | 확인 필요 사항 |
|---|---|---|
| 여행기간 표기 | 계약서의 여행상품명에는 "8박 10일"로 표기되어 있으나, 실제 출발일~도착일을 계산하면 **9박 10일**입니다 | 상품명 오타인지, 일정표 자체가 8박인지 확인 필요 (ERP에서는 이런 불일치를 막기 위해 "박수"를 수기 입력하지 않고 출발일·도착일에서 자동 계산해야 함) |
| 환율 계산 셀 수식 오류 | 계약서 시트의 환율 관련 셀(N6) 수식이 `=N4*N6*1309.2`로, **자기 자신(N6)을 참조하는 순환참조**로 되어 있어 정상적으로 계산되지 않습니다(다른 셀은 `=N4*N5` 형태) | 원래 의도한 수식이 `=N4*N5*1309.2` 또는 `=N4*N5`인지 확인 필요 — ERP 이전(마이그레이션) 전에 원본 수식부터 바로잡아야 합니다 |
| 발신 회사명 불일치 | `예약취소의뢰서`·`보험가입` 시트의 발신란이 "쥬얼리모두투어"로 되어 있어, 계약서상 회사명(㈜아일항공여행사)과 다릅니다 | 과거 다른 소속사 시절 템플릿이 그대로 남아있는 것으로 보입니다. ERP 도입 시 발신 회사명은 회사 마스터 정보에서 자동으로 채워지도록 해야 재발 방지됩니다 |
| 취소의뢰서 내 잔존 샘플 데이터 | `예약취소의뢰서` 시트의 리조트/풀빌라·날짜(2019년, 방콕 노보텔/반얀트리)가 이번 건(2027년 파리·포르투갈)과 전혀 무관한 이전 건 데이터로 남아있습니다 | 서식을 재사용할 때 이전 건 데이터를 지우지 않고 덮어쓰기만 해서 발생한 것으로 보입니다. ERP에서는 건마다 새 레코드가 생성되므로 이런 잔존 데이터 문제가 구조적으로 사라집니다 |
| 보험가입 국가 불일치 | `보험가입` 시트의 국가란이 "태국"으로 되어 있어, 실제 목적지(유럽 파리·포르투갈)와 다릅니다 | 위와 동일하게 이전 건 잔존 데이터로 추정 — ERP에서는 목적지가 예약 건에서 자동으로 채워져야 함 |
| 1인요금/총액 컬럼 혼용 | `예약및정산` 시트의 "판매가·계약가" 컬럼은 1인 기준 금액이고 "총요금·합계" 컬럼은 2인 총액인데, 같은 표 안에 기준이 다른 금액이 섞여 있어 헷갈리기 쉽습니다 | ERP 화면에서는 "1인 기준"과 "총액"을 별도 컬럼/뱃지로 명확히 구분 표시하는 것을 권장합니다 |

> 위 항목들은 이번 특정 건의 오탈자·잔존데이터일 가능성이 높지만, **서식을 그대로 디지털화하면 같은 실수가 시스템에도 반복**될 수 있으므로 ERP 설계에는 이를 원천 차단하는 규칙(자동계산, 마스터 참조, 건별 신규 레코드)을 반영했습니다.

---

## 9. 기존 ops_* 시스템(허니트립 홈페이지 Supabase) 점검 결과

**새로 설계할 필요가 없었습니다.** `허니트립 홈페이지` Supabase 프로젝트(`zdcwtspzgvtxdsqvjvqv`)에 상담-견적-예약-계약-예약의뢰-정산 전 과정을 다루는 `ops_*` 테이블이 이미 구축되어 있고, 실제 예약 409건·정산입금 890건·지출 1,237건 등 운영 데이터가 쌓여 있습니다. 다만 **단계별로 실사용 여부에 큰 차이**가 있어, 이를 점검한 결과를 남깁니다.

### 9-1. 단계별 실사용 현황

| 우리가 기획한 단계 | 실제 테이블 | 데이터 건수 | 실사용 평가 |
|---|---|---|---|
| ① 상담기록 | `ops_consultations` | 3건 | 거의 미사용 — 대부분 예약이 상담 단계를 거치지 않고 바로 등록됨 |
| ② 견적서 | `ops_quotes` / `ops_quote_versions` / `ops_quote_items` | 1 / 1 / 2건 | 사실상 미사용(파일럿 수준) |
| ③ 예약 | `ops_reservations`(80여 개 컬럼) | 409건 | **활발히 사용 중** — 시스템의 실제 중심 |
| ④ 계약서(구조화) | `ops_contracts` | 1건 | 사실상 미사용 — 아래 9-3 참고 |
| ⑤ 확정 예약(항공/호텔/랜드) | `ops_air_bookings` / `ops_hotel_bookings` / `ops_land_bookings` | 328 / 0 / 0건 | 항공만 부분 사용, 호텔·랜드는 미사용 |
| ⑥ 예약의뢰서 | `ops_reservation_requests` / `_items` | 0 / 0건 | **전혀 사용되지 않음** |
| ⑦ 정산 | `ops_payments` / `ops_expenses` | 890 / 1,237건 | **활발히 사용 중** |
| (부가) 업무관리 | `ops_reservation_tasks` / `ops_reservation_changes` | 1,228 / 6,825건 | 활발히 사용 중 |

**결론**: 예약·정산·업무관리는 이미 시스템으로 잘 돌아가고 있지만, 저희가 처음 기획했던 **①상담기록 → ②견적서 → ④계약서(구조화) → ⑥예약의뢰서** 단계는 테이블 설계는 끝나 있는데 현장에서 쓰이지 않고 있습니다. 즉 지금 필요한 건 "새 설계"가 아니라 **"이미 있는 화면·기능을 상담원이 실제로 쓰게 만드는 것"** 입니다.

### 9-2. 검증용 실사례 교차 확인

지난번 검토했던 실사용 엑셀(이지훈·최효정 고객, 파리+포르투갈 건)이 이미 이 시스템에 `reservation_code: HM-XLSX-0305`로 이관되어 있는 것을 확인했습니다. 이를 통해 두 가지가 교차 검증되었습니다.

- 엑셀에서 지적했던 **"8박 10일" 표기 오류**가 `duration_text` 컬럼에 그대로 남아 있습니다(실제는 9박 10일). 아래 9-3에서 보듯 이 건만의 오탈자가 아니라 **시스템 전반의 반복 패턴**임이 확인되었습니다.
- 최초 계약 금액(6,160,000원) 이후 실제 판매가가 9,399,800원으로 변경되었으나, 이를 반영한 새 계약서 버전이 생성되지 않아 **현재 이 예약은 "활성 계약서 없음"** 상태입니다(`active_contract_id`가 비어 있음).

### 9-3. 전사 데이터 품질 점검 결과 (확인 필요)

| 이슈 | 규모 | 내용 |
|---|---|---|
| 여행일수(박/일) 표기 오류 | 예약 409건 중 **317건(약 77%)** | `duration_text`가 실제 출발~도착일 차이보다 "1박" 적게 표기되어 있습니다(예: 실제 9박10일인데 "8박10일"로 표기). 엑셀 시절부터의 수기입력 관행이 그대로 시스템에 이관·반복되고 있는 것으로 보입니다 |
| 출발일보다 도착일이 빠른 예약 | **9건** | 도착일의 연도가 출발일보다 1년 이상 앞서 있어 물리적으로 불가능한 일정으로 저장되어 있습니다(연도 오타로 추정). 예약코드: HM-XLSX-0008, 0017, 0118, 0131, 0139, 0157, 0216, 0237, 0248 |
| 계약서 버전 갱신 체계 미정착 | 조직 전체 306건 | `ops_reservations.contract_written`(계약서 작성함) 표시는 되어 있지만, 이 중 305건은 구조화된 계약 레코드(`ops_contracts`)가 아예 없습니다. 계약서 자체는 시스템 밖(수기·PDF)에서 작성되고 시스템에는 체크박스만 남기는 방식으로 운영 중인 것으로 보입니다 |
| 환율 조항 데이터 미기록 | 확인한 유럽 상품 건 | `contract_exchange_rate`/`balance_exchange_rate`/`fx_currency` 컬럼이 모두 비어 있습니다 — 엑셀에서 환율 수식이 순환참조 오류였던 것과 같은 맥락으로, 환율 조항이 구조화 데이터로 관리되지 않고 있습니다 |

> 위 항목은 시스템 결함이라기보다 **"컬럼은 있지만 입력 프로세스가 정착되지 않은" 상태**에 가깝습니다. `duration_text`처럼 자동계산으로 바꿀 수 있는 항목은 구조적으로 오류를 없앨 수 있습니다.

### 9-4. 보안 점검 (Supabase Advisor)

- (기존 안내) `honeytrip_destination_match_profiles` 등 4개 테이블은 RLS 자체가 꺼져 있어 익명 키로 읽기/쓰기가 가능합니다.
- `ops_*` 관련 대시보드 뷰 6개(`ops_work_dashboard_summary`, `ops_work_dashboard_summary_v2`, `ops_today_work_center_active/core/ui`, `ops_reservation_progress`, `ops_finance_dashboard_summary`, `ops_staff_task_dashboard`)가 **SECURITY DEFINER**로 정의되어 있어, 조회자 권한이 아니라 "만든 사람 권한"으로 실행됩니다. 뷰 내부에 조직·권한 필터가 없다면 권한이 낮은 직원도 전체 데이터를 볼 수 있는 우회 경로가 될 수 있어 **확인이 필요**합니다.
- 반대로 `ops_reservations`/`ops_contracts`/`ops_quotes`/`ops_payments`/`ops_expenses`/`ops_travelers` 등 핵심 테이블은 `authenticated` 역할과 조직 단위 권한 함수(`private.ops_has_permission`, `private.ops_is_master`)로 잘 통제되고 있어 **설계 자체는 양호**합니다.
- (참고, 이번 ERP와는 별개) 같은 프로젝트 내 칸쿤(Cancun) 관련 관리자 함수 일부가 비로그인 상태(anon)에서도 실행 가능하도록 노출되어 있어, 별도로 점검이 필요해 보입니다.

### 9-5. 권장 다음 단계

1. **(가장 시급, 비용 없음)** `duration_text`를 수기 입력 대신 출발일·도착일로부터 자동 계산되는 값으로 전환 — 새 오류 발생을 구조적으로 차단
2. 날짜 순서가 뒤바뀐 예약 9건의 연도 오탈자 수정
3. 이미 잘 설계되어 있는 `ops_quotes`(견적서)·`ops_contracts`(계약서)·`ops_reservation_requests`(예약의뢰서) 화면을 상담원이 매일 쓰는 업무 흐름에 실제로 연결 — 지금처럼 엑셀과 시스템이 따로 노는 상태를 해소
4. 계약 금액이 변경될 때 `ops_contracts`에 새 버전을 자동 생성하고 `active_contract_id`를 갱신하는 로직 추가
5. SECURITY DEFINER 대시보드 뷰의 내부 필터 확인, 미보호 테이블 4개에 RLS 정책 설계

---

## 10. 9-5장 후속조치 진행 내역 (DB 레벨 완료분)

`허니트립 홈페이지` Supabase 프로젝트(`zdcwtspzgvtxdsqvjvqv`)에 아래 내용을 실제로 적용했습니다. 모두 마이그레이션으로 기록되어 있어 필요 시 추적 가능합니다.

### ① `duration_text` 자동계산 전환 — 완료
- 출발일·도착일로부터 "N박M일"을 자동 계산하는 함수(`ops_compute_duration_text`)와, 예약 등록·수정 시 이를 자동으로 채우는 트리거(`trg_ops_reservations_duration_text`)를 추가했습니다.
- 기존 오류 317건을 일괄 정정했습니다. 이제 이 컬럼에 수기로 잘못된 값을 넣어도 저장 시점에 자동으로 올바른 값으로 대체됩니다.

### ② 날짜 순서 오류 9건 수정 — 완료
- 도착일의 "연도"만 출발일 연도와 어긋나 있던 패턴(월·일은 정상)으로 확인, 9건 모두 연도를 출발일 기준으로 정정했습니다.
- 정정 전 원본 값은 `ops_reservation_changes`(변경이력 테이블)에 남겨두었으니, 예약코드 HM-XLSX-0008 / 0017 / 0118 / 0131 / 0139 / 0157 / 0216 / 0237 / 0248 은 필요 시 원본 데이터를 대조 확인하시길 권장합니다(특히 0008, 0248은 연도 격차가 커서 재확인이 더 필요합니다).

### ③ 견적서/계약서/예약의뢰서 화면 연결 — 12장 참고 (재조사 결과 이미 구현되어 있었음)
- 최초에는 저장소가 없어 보류했으나, 실제 저장소(`ail-air-tour-operation-center`)를 확인해 보니 **이미 세 화면 모두 코드로 구현되어 있었습니다.** 자세한 내용은 12장 참고.

### ④ 계약 금액 변경 시 자동 버전 갱신 — 적용 후 되돌림 (12장 참고)
- 처음에는 판매가가 바뀌면 자동으로 새 계약 버전을 만드는 트리거(`trg_ops_reservations_revision_contract`)를 추가했으나, 실제 앱(`ContractWorkspace.jsx`)이 이미 "차액 안내 + 상담원 수동 검토(`ops_source_reconciliations`)" 방식으로 설계되어 있어 **자동 트리거가 이 검토 과정과 충돌**한다는 것을 확인하고 되돌렸습니다.
- 9-2장에서 발견했던 결측 건(HM-XLSX-0305, 판매가가 6,160,000원→9,399,800원으로 바뀌었는데 활성 계약서가 없던 상태)의 복구(계약서 v2 생성)는 그대로 유지했습니다(상태값만 앱이 인식하는 `signed`로 정정).

### ⑤ SECURITY DEFINER 대시보드 뷰 점검 — 완료
- `ops_work_dashboard_summary`, `ops_finance_dashboard_summary`, `ops_reservation_progress`, `ops_today_work_center_*`(4종), `ops_staff_task_dashboard`, `ops_staff_task_worklist` 등 11개 뷰를 확인한 결과, 조직(organization_id)별로 집계만 할 뿐 자체 필터가 없어 SECURITY DEFINER 상태에서는 **인증된 직원이면 다른 조직의 예약 현황·미수금·수익까지 조회 가능**한 구조였습니다.
- 11개 뷰 모두 `SECURITY INVOKER`로 전환해, 조회자 본인의 조직 권한(RLS)이 그대로 적용되도록 수정했습니다.
- 참고(이번 범위 밖): 같은 프로젝트 내 4개 테이블 RLS 미설정, 칸쿤(Cancun) 관리자 함수 일부의 익명 접근 노출은 여전히 남아있어 별도 확인이 필요합니다.

---

## 12. ③번 재조사 결과: 이미 구현되어 있었음 (`ail-air-tour-operation-center`)

허니트립 운영센터(`ops.honeytrip.co.kr`)의 실제 저장소는 `ail-travel-main`(고객용 홈페이지)이 아니라 **`yoojiha85-tech/ail-air-tour-operation-center`** 였습니다. 이 저장소를 연결해 확인한 결과, 아래와 같이 **견적서·계약서·예약의뢰서 화면이 이미 모두 구현되어 있었습니다.**

| 화면 | 파일 | 상태 |
|---|---|---|
| 견적관리 | `src/modules/Quote/QuoteWorkspace.jsx` | 완성 — 견적 다안(V1~) 작성, 버전 관리, 고객 선택안 확정까지 정상 구현 |
| 계약관리 | `src/modules/Contract/ContractWorkspace.jsx` | 완성 — 선택 견적을 계약 스냅샷으로 고정, 계약금/중도금/잔금 계산, 계약 완료 처리 |
| 예약의뢰서 | `src/modules/ReservationCase/ReservationCaseControlCenter.jsx` | 완성 — 계약 완료 후 항공/호텔/랜드 예약의뢰 생성·확정 관리 |
| 원본 대조 | `src/modules/ReservationCase/ReconciliationPanel.jsx` + `ops_source_reconciliations` | 완성 — 원본 자료와 운영 데이터 차이를 상담원이 직접 검토·처리하는 화면 |

가장 최근 커밋이 "MASTER ERP Phase 2: CASE Dashboard + D-Day Automation"으로, 이 화면들은 **최근에 새로 만들어져 아직 실사용 이력이 거의 없는 상태(9-1장에서 확인한 데이터 건수 1~2건)** 로 보입니다. 즉 9장에서 "설계는 있는데 안 쓰인다"고 판단했던 부분은 정확히는 **"이미 만들어졌지만 아직 현장 도입 전"** 인 상태였습니다.

이 사실을 확인하면서, 4번 작업에서 제가 추가했던 자동 트리거가 `ContractWorkspace.jsx`의 의도된 수동 검토 흐름과 충돌한다는 것도 함께 발견해 되돌렸습니다(위 10장 ④ 참고).

### 확인이 필요한 사항 → 14장에서 확인 완료
- 배포 여부와 실사용 건수가 적은 이유는 14장에서 확인했습니다: **오늘이 정식 오픈 시점**이고, 기존에 있던 데이터는 예전 예약 건을 옮겨 놓은 것이라 견적/계약/예약의뢰 신규 이력이 적은 게 정상입니다.

---

## 14. 배포 점검 · QA 결과 (오픈 당일)

오늘이 이 시스템의 정식 운영 시작일이라는 말씀에 따라, 배포 상태 점검과 최근 추가된 Phase 2 코드에 대한 QA를 진행했습니다.

### 14-1. 배포 점검 — 정상

- `https://ops.honeytrip.co.kr`(Netlify 프로젝트 `ailair`)가 저장소의 최신 커밋(`7492930`, "MASTER ERP Phase 2")과 **정확히 일치하는 코드로 배포되어 있음을 확인**했습니다.
- 배포 상태 정상(ready), 시크릿 스캔 이상 없음(0건), 프로덕션 컨텍스트로 정상 배포.
- 즉 지금 상담원분들이 접속하시는 화면은 제가 검토한 최신 코드가 맞습니다.

### 14-2. 코드 QA에서 발견한 사항

| 심각도 | 내용 | 조치 |
|---|---|---|
| **중요 (수정 완료)** | 웹 상담을 예약으로 전환하는 함수(`convert_consultation_to_reservation`)가 CASE 진행 단계(`case_stage`)를 지정하지 않아, 방금 전환되어 견적·계약·예약의뢰가 전혀 없는 신규 예약이 **CASE Control Center 진행바에서 상담·견적·계약·예약의뢰가 이미 끝난 것처럼** 표시되는 문제를 발견했습니다. 오늘부터 들어오는 신규 상담이 첫 사용 사례라 바로 영향받는 부분이었습니다. | 상담 완료 후 다음 단계인 "견적"으로 명시 설정하도록 DB 함수를 수정하고, 프로덕션에 즉시 적용 + 저장소에 마이그레이션 커밋·푸시 완료 |
| 참고 (당장 문제 없음) | `ops_reservation_tasks` 테이블에 "전체 유니크"와 "자동생성 항목만 유니크"인 인덱스가 동시에 존재합니다. 현재는 모든 업무 항목이 자동생성이라 문제가 없지만, 향후 상담원이 같은 이름의 업무를 수동으로 추가하는 기능이 생기면 자동 동기화 트리거가 예약 저장 시 오류를 낼 수 있는 구조입니다. | 지금 당장 발생하는 문제는 아니라 조치하지 않았습니다. 수동 업무 추가 기능을 만드실 때 참고해 주세요 |

### 14-3. 확인된 양호한 설계 (참고)

- 견적서(`ops_quotes`)와 계약서(`ops_contracts`)는 각각 "선택된 견적 1건", "활성 계약 1건"만 존재하도록 DB 레벨 유니크 제약으로 강제되어 있어, 화면 로직에 오류가 있어도 데이터가 꼬이지 않도록 이중으로 보호되어 있습니다.
- 상담 → 예약 전환 함수는 동시에 두 번 전환되지 않도록 행 잠금을 사용하고, 권한 체크와 예약번호 중복 처리까지 되어 있어 안전합니다.
- CASE 대시보드 뷰는 이미 `security_invoker`로 만들어져 있어, 이전에 발견했던 것과 같은 "다른 조직 데이터 노출" 문제가 없습니다.
- 정산 화면(`FinanceSummary.jsx`)은 조회 권한이 없는 사용자에게 원가·수익 정보를 아예 보여주지 않도록 되어 있습니다.

---

## 15. 다음 액션 제안

1. ~~상담기록 양식과 견적서 양식 공유~~ → 계약서·예약및정산·예약의뢰서 4종·보험가입 서식은 확보 완료. **상담 접수 시 사용하는 최초 상담일지(문의 접수) 양식**이 있다면 추가로 공유해 주시면 `consultations` 테이블 컬럼까지 100% 확정할 수 있습니다.
2. 위 8-4 표의 확인 필요 사항(발신 회사명 등)에 대한 답을 주시면 반영하겠습니다.
3. ~~배포·QA 점검~~ → 14장에서 완료. 오픈 당일 발견된 CASE 단계 표시 버그는 수정·배포까지 완료했습니다.
4. 10장에서 정정한 날짜 9건 중 특히 연도 격차가 컸던 2건(HM-XLSX-0008, 0248)은 원본 예약 기록(카톡·통화 이력 등)과 한 번 대조해 주시면 확실합니다.
5. `ops_reservation_tasks`의 수동 업무 추가 기능을 나중에 만드실 계획이 있다면, 14-2장의 유니크 인덱스 이슈를 미리 함께 정리해드릴 수 있습니다.
